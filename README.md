# template_cli
test query API:

# usage
```
$ python path/to/dir/main.py --help
usage: main.py [-h] --api-key API_KEY --domain DOMAIN [--proxies PROXIES]

options:
  -h, --help                     show this help message and exit
  --api-key API_KEY              The API key
  --domain DOMAIN                The FQDN for your API (not full URL)
  --proxies PROXIES              JSON structure specifying 'http' and 'https' proxy URLs
  --log-level                    0 (NOTSET) - 50 (CRITICAL)
```
